[project]
name = "information-visualization-project"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "notebook>=7.5.5",
    "pandas>=3.0.2",
]

[tool.uv.workspace]
members = [
    "app",
]
